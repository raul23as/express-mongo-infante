<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title><%= title %> | AuthApp</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600;700&display=swap" rel="stylesheet"/>
  <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet"/>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css"/>
  <style>
    :root { --primary:#1a237e; --accent:#42a5f5; --danger:#e53935; --success:#43a047; }
    body  { font-family:'Poppins',sans-serif; background:#f4f6fb; min-height:100vh; }
    nav   { background:var(--primary); }
    nav .brand-logo { font-weight:700; letter-spacing:1px; }
    .btn  { background:var(--primary); border-radius:6px; font-weight:600; }
    .btn:hover { background:#283593; }
    .card { border-radius:12px; }
  </style>
</head>
<body>
